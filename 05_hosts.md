Add to your home directory a custom ansible configuration file.


```bash
vim ~/.ansible.cfg
```

Adjust path to inventory file
```
[defaults]
inventory = /demos/life_of_pi/hosts
```

Create inventory, see [hosts](hosts)
